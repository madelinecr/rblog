#main-title {
  background-image: url(<%= asset_path "background.png" %>);
  background-size: cover;
}

#title-bar {
  background-color: rgba(0, 0, 0, 0.5);
  color: #fff;
}

#title-bar .subheader {
  color: #f0f0f0;
}
